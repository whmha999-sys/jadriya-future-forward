

## Plan: Make Chatbot Logo Fill the Button

### Change
In `src/components/ChatBot.tsx` line 93, change the logo image class from `w-9 h-9 object-contain` to `w-full h-full object-cover rounded-full` so the logo image fills 100% of the button circle while the button itself stays the same size (w-14 h-14).

